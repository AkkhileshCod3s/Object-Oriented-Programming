# JAVA EXCEPTION HANDLING CODING PRACTICE SET • 30 QUESTIONS

## Topics Covered
- `try`
- `catch`
- `finally`
- multiple `catch`
- nested `try-catch`
- `throw`
- `throws`
- checked exceptions
- unchecked exceptions
- built-in/predefined exception classes
- custom/user-defined exception classes
- custom exception with predefined class + `main()`
- custom exception where the student must create the exception class
- exception propagation
- rethrowing exceptions
- multiple exceptions
- practical validation problems

> **Format:** Every question contains a complete predefined code skeleton. Write your solution only in the marked sections.  
> **Note:** All code in this file uses one consistent Markdown/code-block style.

---

# SECTION A — try, catch, finally & Predefined Exceptions
## Questions 1–10

---

## Q1. Safe Integer Division

**Difficulty:** EASY  
**Concept:** `try-catch`, `ArithmeticException`

### PROBLEM STATEMENT
Read two integers and divide the first number by the second. Handle division by zero using `try-catch`. If division is possible, print the result; otherwise print `Cannot divide by zero`.

### CONSTRAINTS
- Input values are integers.
- The second number may be zero.

### PREDEFINED CODE
```java
import java.util.*;

public class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        int a = sc.nextInt();
        int b = sc.nextInt();

        try {
            // Perform division and print the result
        } catch (ArithmeticException e) {
            // Handle division by zero
        }
    }
}
```

### SAMPLE INPUT
```text
20 5
```

### SAMPLE OUTPUT
```text
4
```

### EXPLANATION
Practice catching the predefined `ArithmeticException` generated by integer division by zero.

**Expected Complexity:** Time: O(1), Space: O(1)

---

## Q2. Array Index Validation

**Difficulty:** EASY  
**Concept:** `try-catch`, `ArrayIndexOutOfBoundsException`

### PROBLEM STATEMENT
Read N integers into an array and then read an index. Print the element at that index. If the index is invalid, print `Invalid Index`.

### CONSTRAINTS
- `1 <= N <= 1000`
- The index may be outside the valid range.

### PREDEFINED CODE
```java
import java.util.*;

public class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        int n = sc.nextInt();
        int[] arr = new int[n];

        for (int i = 0; i < n; i++) {
            arr[i] = sc.nextInt();
        }

        int index = sc.nextInt();

        try {
            // Access and print arr[index]
        } catch (ArrayIndexOutOfBoundsException e) {
            // Handle invalid index
        }
    }
}
```

### SAMPLE INPUT
```text
4
10 20 30 40
5
```

### SAMPLE OUTPUT
```text
Invalid Index
```

### EXPLANATION
The array access can generate a predefined `ArrayIndexOutOfBoundsException`.

**Expected Complexity:** Time: O(N), Space: O(N)

---

## Q3. Null String Length

**Difficulty:** EASY  
**Concept:** `try-catch`, `NullPointerException`

### PROBLEM STATEMENT
A string reference is intentionally set to `null`. Attempt to print its length and handle the resulting exception.

### PREDEFINED CODE
```java
public class Main {
    public static void main(String[] args) {
        String text = null;

        try {
            // Print the length of text
        } catch (NullPointerException e) {
            // Print "String is null"
        }
    }
}
```

### SAMPLE OUTPUT
```text
String is null
```

### EXPLANATION
Calling a method on a `null` reference causes `NullPointerException`.

**Expected Complexity:** Time: O(1), Space: O(1)

---

## Q4. String to Integer Conversion

**Difficulty:** EASY  
**Concept:** `NumberFormatException`

### PROBLEM STATEMENT
Read a string and convert it to an integer using `Integer.parseInt()`. If the string does not contain a valid integer, print `Invalid Number`.

### PREDEFINED CODE
```java
import java.util.*;

public class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        String value = sc.next();

        try {
            // Convert value to integer and print it
        } catch (NumberFormatException e) {
            // Handle invalid numeric input
        }
    }
}
```

### SAMPLE INPUT
```text
12abc
```

### SAMPLE OUTPUT
```text
Invalid Number
```

### EXPLANATION
`Integer.parseInt()` throws the predefined `NumberFormatException` for invalid numeric strings.

**Expected Complexity:** Time: O(N), Space: O(1)

---

## Q5. Multiple Catch Blocks

**Difficulty:** MEDIUM  
**Concept:** Multiple `catch` blocks

### PROBLEM STATEMENT
Read two integers. The program should perform array access followed by division. Handle `ArrayIndexOutOfBoundsException` and `ArithmeticException` using separate `catch` blocks.

### PREDEFINED CODE
```java
import java.util.*;

public class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        int[] arr = {10, 20, 30};

        int index = sc.nextInt();
        int divisor = sc.nextInt();

        try {
            // Access arr[index]
            // Divide the accessed value by divisor
            // Print the result
        } catch (ArrayIndexOutOfBoundsException e) {
            // Handle invalid index
        } catch (ArithmeticException e) {
            // Handle division by zero
        }
    }
}
```

### SAMPLE INPUT
```text
1 0
```

### SAMPLE OUTPUT
```text
Cannot divide by zero
```

### EXPLANATION
Different exceptions can be handled by different `catch` blocks.

**Expected Complexity:** Time: O(1), Space: O(1)

---

## Q6. finally Block Execution

**Difficulty:** EASY  
**Concept:** `finally`

### PROBLEM STATEMENT
Read two integers and perform division. Whether the division succeeds or fails, the program must always print `Program Finished` using a `finally` block.

### PREDEFINED CODE
```java
import java.util.*;

public class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        int a = sc.nextInt();
        int b = sc.nextInt();

        try {
            // Perform division and print the result
        } catch (ArithmeticException e) {
            // Print "Cannot divide by zero"
        } finally {
            // Print "Program Finished"
        }
    }
}
```

### SAMPLE INPUT
```text
10 0
```

### SAMPLE OUTPUT
```text
Cannot divide by zero
Program Finished
```

### EXPLANATION
The `finally` block executes after the `try`/`catch` flow whether an exception occurs or not.

**Expected Complexity:** Time: O(1), Space: O(1)

---

## Q7. Multiple Catch with finally

**Difficulty:** MEDIUM  
**Concept:** multiple `catch` + `finally`

### PROBLEM STATEMENT
Read an index and a numeric string. First access an array element and then convert the string to an integer. Handle both possible exceptions and always print `Execution Completed`.

### PREDEFINED CODE
```java
import java.util.*;

public class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        int[] arr = {5, 10, 15};

        int index = sc.nextInt();
        String value = sc.next();

        try {
            // Access arr[index]
            // Convert value to integer
            // Print both values
        } catch (ArrayIndexOutOfBoundsException e) {
            // Handle invalid index
        } catch (NumberFormatException e) {
            // Handle invalid number
        } finally {
            // Print "Execution Completed"
        }
    }
}
```

### SAMPLE INPUT
```text
5 20
```

### SAMPLE OUTPUT
```text
Invalid Index
Execution Completed
```

### EXPLANATION
This combines separate exception handlers with a mandatory cleanup/finalization section.

**Expected Complexity:** Time: O(1), Space: O(1)

---

## Q8. Nested try-catch

**Difficulty:** MEDIUM  
**Concept:** Nested `try-catch`

### PROBLEM STATEMENT
Use an outer `try-catch` and an inner `try-catch`. The inner block should handle division by zero. The outer block should handle invalid array access.

### PREDEFINED CODE
```java
import java.util.*;

public class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        int[] arr = {100, 200, 300};
        int index = sc.nextInt();
        int divisor = sc.nextInt();

        try {
            // Access arr[index]

            try {
                // Divide the selected value by divisor
                // Print result
            } catch (ArithmeticException e) {
                // Handle division by zero
            }

        } catch (ArrayIndexOutOfBoundsException e) {
            // Handle invalid index
        }
    }
}
```

### SAMPLE INPUT
```text
1 0
```

### SAMPLE OUTPUT
```text
Cannot divide by zero
```

### EXPLANATION
Nested `try-catch` blocks allow different levels of the program to handle different exceptions.

**Expected Complexity:** Time: O(1), Space: O(1)

---

## Q9. Generic Exception Handling

**Difficulty:** MEDIUM  
**Concept:** `Exception` superclass

### PROBLEM STATEMENT
Read a string and convert it to an integer. Catch the error using the general `Exception` class instead of a specific exception class.

### PREDEFINED CODE
```java
import java.util.*;

public class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        String value = sc.next();

        try {
            // Convert value to integer and print it
        } catch (Exception e) {
            // Print "Exception Occurred"
        }
    }
}
```

### SAMPLE INPUT
```text
abc
```

### SAMPLE OUTPUT
```text
Exception Occurred
```

### EXPLANATION
`Exception` can catch many exception types, although specific exception classes are usually preferred when possible.

**Expected Complexity:** Time: O(N), Space: O(1)

---

## Q10. Exception Message

**Difficulty:** MEDIUM  
**Concept:** `catch`, `getMessage()`

### PROBLEM STATEMENT
Perform integer division. If division by zero occurs, print the exception message using `e.getMessage()`.

### PREDEFINED CODE
```java
import java.util.*;

public class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        int a = sc.nextInt();
        int b = sc.nextInt();

        try {
            // Perform and print a / b
        } catch (ArithmeticException e) {
            // Print e.getMessage()
        }
    }
}
```

### SAMPLE INPUT
```text
10 0
```

### SAMPLE OUTPUT
```text
/ by zero
```

### EXPLANATION
Exception objects provide useful information through methods such as `getMessage()`.

**Expected Complexity:** Time: O(1), Space: O(1)

---

# SECTION B — throw and throws with Predefined Exceptions
## Questions 11–18

---

## Q11. Validate Positive Number using throw

**Difficulty:** MEDIUM  
**Concept:** `throw`

### PROBLEM STATEMENT
Create a method `checkNumber()` that throws an `IllegalArgumentException` if the given number is less than or equal to zero. Otherwise print `Valid Number`.

### PREDEFINED CODE
```java
import java.util.*;

public class Main {

    static void checkNumber(int n) {
        // Use throw when n is invalid
    }

    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        int n = sc.nextInt();

        try {
            checkNumber(n);
            System.out.println("Valid Number");
        } catch (IllegalArgumentException e) {
            System.out.println("Invalid Number");
        }
    }
}
```

### SAMPLE INPUT
```text
-5
```

### SAMPLE OUTPUT
```text
Invalid Number
```

### EXPLANATION
`throw` explicitly creates and sends an exception from the method.

**Expected Complexity:** Time: O(1), Space: O(1)

---

## Q12. Validate Age using throw

**Difficulty:** MEDIUM  
**Concept:** `throw`, `IllegalArgumentException`

### PROBLEM STATEMENT
Create a method `validateAge(int age)`. Throw `IllegalArgumentException` if age is below 0 or above 120. Otherwise print `Valid Age`.

### PREDEFINED CODE
```java
import java.util.*;

public class Main {

    static void validateAge(int age) {
        // Validate age using throw
    }

    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        int age = sc.nextInt();

        try {
            validateAge(age);
            System.out.println("Valid Age");
        } catch (IllegalArgumentException e) {
            System.out.println("Invalid Age");
        }
    }
}
```

### SAMPLE INPUT
```text
150
```

### SAMPLE OUTPUT
```text
Invalid Age
```

### EXPLANATION
This question practices explicitly throwing a predefined unchecked exception for invalid data.

**Expected Complexity:** Time: O(1), Space: O(1)

---

## Q13. Method using throws

**Difficulty:** MEDIUM  
**Concept:** `throws`

### PROBLEM STATEMENT
Create a method `divide()` that declares `throws ArithmeticException`. Perform division inside the method and handle the exception in `main()`.

### PREDEFINED CODE
```java
import java.util.*;

public class Main {

    static int divide(int a, int b) throws ArithmeticException {
        // Return a / b
        return 0;
    }

    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        int a = sc.nextInt();
        int b = sc.nextInt();

        try {
            System.out.println(divide(a, b));
        } catch (ArithmeticException e) {
            System.out.println("Cannot divide by zero");
        }
    }
}
```

### SAMPLE INPUT
```text
20 4
```

### SAMPLE OUTPUT
```text
5
```

### EXPLANATION
`throws` declares that a method may pass an exception to its caller.

**Expected Complexity:** Time: O(1), Space: O(1)

---

## Q14. Square Root Validation

**Difficulty:** MEDIUM  
**Concept:** `throw`, `throws`

### PROBLEM STATEMENT
Create `calculateSquareRoot(int n)` that declares `throws IllegalArgumentException`. If `n` is negative, throw the exception. Otherwise print its square root.

### PREDEFINED CODE
```java
import java.util.*;

public class Main {

    static double calculateSquareRoot(int n)
            throws IllegalArgumentException {

        // Throw IllegalArgumentException for negative n
        // Return Math.sqrt(n)
        return 0;
    }

    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        int n = sc.nextInt();

        try {
            System.out.println("Square Root: "
                    + calculateSquareRoot(n));
        } catch (IllegalArgumentException e) {
            System.out.println("Invalid Number");
        }
    }
}
```

### SAMPLE INPUT
```text
25
```

### SAMPLE OUTPUT
```text
Square Root: 5.0
```

### EXPLANATION
This is a direct combination of `throw` and `throws` using a predefined exception class.

**Expected Complexity:** Time: O(1), Space: O(1)

---

## Q15. Password Length Validation

**Difficulty:** MEDIUM  
**Concept:** `throw`, `IllegalArgumentException`

### PROBLEM STATEMENT
Create a method that throws `IllegalArgumentException` when a password contains fewer than 8 characters.

### PREDEFINED CODE
```java
import java.util.*;

public class Main {

    static void validatePassword(String password) {
        // Throw IllegalArgumentException if length < 8
    }

    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        String password = sc.next();

        try {
            validatePassword(password);
            System.out.println("Valid Password");
        } catch (IllegalArgumentException e) {
            System.out.println("Password Too Short");
        }
    }
}
```

### SAMPLE INPUT
```text
abc123
```

### SAMPLE OUTPUT
```text
Password Too Short
```

### EXPLANATION
Validation logic can explicitly throw a predefined exception when a business rule fails.

**Expected Complexity:** Time: O(N), Space: O(1)

---

## Q16. Parse Integer using throws

**Difficulty:** MEDIUM  
**Concept:** `throws`, `NumberFormatException`

### PROBLEM STATEMENT
Create a method `convertToInt()` that declares `throws NumberFormatException` and returns the integer representation of a string.

### PREDEFINED CODE
```java
import java.util.*;

public class Main {

    static int convertToInt(String value)
            throws NumberFormatException {
        // Convert and return value
        return 0;
    }

    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        String value = sc.next();

        try {
            System.out.println(convertToInt(value));
        } catch (NumberFormatException e) {
            System.out.println("Invalid Integer");
        }
    }
}
```

### SAMPLE INPUT
```text
45
```

### SAMPLE OUTPUT
```text
45
```

### EXPLANATION
The method declares the possible predefined exception and lets the caller handle it.

**Expected Complexity:** Time: O(N), Space: O(1)

---

## Q17. Multiple Validation Exceptions

**Difficulty:** HARD  
**Concept:** multiple `throw`, multiple `catch`

### PROBLEM STATEMENT
Validate a student's marks and age. Throw `IllegalArgumentException` if marks are outside 0–100 and throw `ArithmeticException` if age is zero. Handle both separately.

### PREDEFINED CODE
```java
import java.util.*;

public class Main {

    static void validate(int marks, int age) {
        // Throw IllegalArgumentException for invalid marks
        // Throw ArithmeticException when age is zero
    }

    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        int marks = sc.nextInt();
        int age = sc.nextInt();

        try {
            validate(marks, age);
            System.out.println("Valid Data");
        } catch (IllegalArgumentException e) {
            System.out.println("Invalid Marks");
        } catch (ArithmeticException e) {
            System.out.println("Invalid Age");
        }
    }
}
```

### SAMPLE INPUT
```text
105 20
```

### SAMPLE OUTPUT
```text
Invalid Marks
```

### EXPLANATION
Different validation failures can deliberately throw different predefined exception types.

**Expected Complexity:** Time: O(1), Space: O(1)

---

## Q18. Exception Propagation through Methods

**Difficulty:** HARD  
**Concept:** `throws`, exception propagation

### PROBLEM STATEMENT
Create three methods: `method1()`, `method2()`, and `method3()`. The exception should originate in `method3()` and propagate through the other methods until it is handled in `main()`.

### PREDEFINED CODE
```java
public class Main {

    static void method3() throws ArithmeticException {
        // Cause division by zero
    }

    static void method2() throws ArithmeticException {
        // Call method3()
    }

    static void method1() throws ArithmeticException {
        // Call method2()
    }

    public static void main(String[] args) {
        try {
            method1();
        } catch (ArithmeticException e) {
            System.out.println("Exception Handled in Main");
        }
    }
}
```

### SAMPLE OUTPUT
```text
Exception Handled in Main
```

### EXPLANATION
An exception can travel upward through the call stack when intermediate methods do not handle it.

**Expected Complexity:** Time: O(1), Space: O(1) excluding call stack

---

# SECTION C — Custom Exception Classes
## Questions 19–30

---

## Q19. Custom Invalid Marks Exception — Class Predefined

**Difficulty:** HARD  
**Concept:** Custom checked exception, `throw`, `throws`

### PROBLEM STATEMENT
Create a custom checked exception named `InvalidMarksException`. If marks are below 0 or above 100, throw it. Otherwise print the marks.

### PREDEFINED CODE
```java
import java.util.*;

class InvalidMarksException extends Exception {
    // Write the custom exception constructor
}

public class Main {

    static void validateMarks(int marks)
            throws InvalidMarksException {

        // Throw InvalidMarksException for invalid marks
    }

    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        int marks = sc.nextInt();

        try {
            validateMarks(marks);
            System.out.println("Valid Marks: " + marks);
        } catch (InvalidMarksException e) {
            System.out.println("Invalid Marks");
        }
    }
}
```

### SAMPLE INPUT
```text
105
```

### SAMPLE OUTPUT
```text
Invalid Marks
```

### EXPLANATION
The custom exception class is already provided. You must complete its constructor and use `throw` and `throws`.

**Expected Complexity:** Time: O(1), Space: O(1)

---

## Q20. Insufficient Balance Exception — Class Predefined

**Difficulty:** HARD  
**Concept:** Custom exception, `throw`, `throws`, class interaction

### PROBLEM STATEMENT
Create a `BankAccount` class. If the withdrawal amount is greater than the balance, throw `InsufficientBalanceException`.

### PREDEFINED CODE
```java
import java.util.*;

class InsufficientBalanceException extends Exception {
    // Write the custom exception constructor
}

class BankAccount {
    private double balance;

    BankAccount(double balance) {
        this.balance = balance;
    }

    void withdraw(double amount)
            throws InsufficientBalanceException {

        // Check balance and throw exception if required
        // Otherwise subtract amount
    }

    double getBalance() {
        return balance;
    }
}

public class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        double balance = sc.nextDouble();
        double amount = sc.nextDouble();

        BankAccount account = new BankAccount(balance);

        try {
            account.withdraw(amount);
            System.out.println("Withdrawal Successful");
            System.out.println("Remaining Balance: "
                    + account.getBalance());
        } catch (InsufficientBalanceException e) {
            System.out.println("Insufficient Balance");
        }
    }
}
```

### SAMPLE INPUT
```text
5000
7000
```

### SAMPLE OUTPUT
```text
Insufficient Balance
```

### EXPLANATION
The custom checked exception represents a domain-specific banking rule.

**Expected Complexity:** Time: O(1), Space: O(1)

---

## Q21. Age Restriction Exception — Class Predefined

**Difficulty:** HARD  
**Concept:** Custom exception with `throws`

### PROBLEM STATEMENT
Create `AgeRestrictionException`. A person must be at least 18 years old to register. Throw the custom exception otherwise.

### PREDEFINED CODE
```java
import java.util.*;

class AgeRestrictionException extends Exception {
    // Write constructor
}

public class Main {

    static void register(int age)
            throws AgeRestrictionException {

        // Throw exception if age < 18
    }

    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        int age = sc.nextInt();

        try {
            register(age);
            System.out.println("Registration Allowed");
        } catch (AgeRestrictionException e) {
            System.out.println("Registration Not Allowed");
        }
    }
}
```

### SAMPLE INPUT
```text
16
```

### SAMPLE OUTPUT
```text
Registration Not Allowed
```

### EXPLANATION
This practices a checked custom exception for an eligibility condition.

**Expected Complexity:** Time: O(1), Space: O(1)

---

## Q22. Negative Deposit Exception — Class Predefined

**Difficulty:** HARD  
**Concept:** Custom exception, `throw`, `throws`

### PROBLEM STATEMENT
Create `InvalidDepositException`. A deposit amount cannot be zero or negative. Throw the custom exception when invalid.

### PREDEFINED CODE
```java
import java.util.*;

class InvalidDepositException extends Exception {
    // Write constructor
}

class Account {
    private double balance = 0;

    void deposit(double amount)
            throws InvalidDepositException {

        // Validate amount and update balance
    }

    double getBalance() {
        return balance;
    }
}

public class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        double amount = sc.nextDouble();
        Account account = new Account();

        try {
            account.deposit(amount);
            System.out.println("Balance: "
                    + account.getBalance());
        } catch (InvalidDepositException e) {
            System.out.println("Invalid Deposit");
        }
    }
}
```

### SAMPLE INPUT
```text
-500
```

### SAMPLE OUTPUT
```text
Invalid Deposit
```

### EXPLANATION
The custom exception is thrown from the account method and caught by `main()`.

**Expected Complexity:** Time: O(1), Space: O(1)

---

## Q23. Custom Exception — Student Must Create the Class

**Difficulty:** HARD  
**Concept:** Creating a custom checked exception from scratch

### PROBLEM STATEMENT
Create a custom exception called `InvalidTemperatureException` by yourself. If temperature is below -100 or above 100, throw it. Otherwise print the temperature.

### PREDEFINED CODE
```java
import java.util.*;

public class Main {

    // Create InvalidTemperatureException here

    static void validateTemperature(int temperature)
            throws InvalidTemperatureException {

        // Write validation logic
    }

    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        int temperature = sc.nextInt();

        try {
            validateTemperature(temperature);
            System.out.println("Valid Temperature");
        } catch (InvalidTemperatureException e) {
            System.out.println("Invalid Temperature");
        }
    }
}
```

### SAMPLE INPUT
```text
150
```

### SAMPLE OUTPUT
```text
Invalid Temperature
```

### EXPLANATION
Unlike previous questions, the exception class is not predefined. You must create a class extending `Exception`.

**Expected Complexity:** Time: O(1), Space: O(1)

---

## Q24. Custom Exception — Salary Validation

**Difficulty:** HARD  
**Concept:** User-defined exception from scratch

### PROBLEM STATEMENT
Create `InvalidSalaryException` yourself. A salary cannot be negative. Throw the exception for invalid salary.

### PREDEFINED CODE
```java
import java.util.*;

public class Main {

    // Create InvalidSalaryException here

    static void validateSalary(double salary)
            throws InvalidSalaryException {

        // Write validation logic
    }

    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        double salary = sc.nextDouble();

        try {
            validateSalary(salary);
            System.out.println("Valid Salary");
        } catch (InvalidSalaryException e) {
            System.out.println("Invalid Salary");
        }
    }
}
```

### SAMPLE INPUT
```text
-10000
```

### SAMPLE OUTPUT
```text
Invalid Salary
```

### EXPLANATION
Create a checked custom exception yourself and use it in a method declaration and `throw` statement.

**Expected Complexity:** Time: O(1), Space: O(1)

---

## Q25. Custom Exception — Password Validation

**Difficulty:** HARD  
**Concept:** Custom exception from scratch, `throw`, `throws`

### PROBLEM STATEMENT
Create `WeakPasswordException` yourself. A password is considered weak if its length is less than 8. Throw the exception for weak passwords.

### PREDEFINED CODE
```java
import java.util.*;

public class Main {

    // Create WeakPasswordException here

    static void validatePassword(String password)
            throws WeakPasswordException {

        // Validate password length
    }

    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        String password = sc.next();

        try {
            validatePassword(password);
            System.out.println("Strong Password");
        } catch (WeakPasswordException e) {
            System.out.println("Weak Password");
        }
    }
}
```

### SAMPLE INPUT
```text
abc123
```

### SAMPLE OUTPUT
```text
Weak Password
```

### EXPLANATION
This question tests the complete flow: create custom class → declare with `throws` → use `throw` → catch in `main()`.

**Expected Complexity:** Time: O(N), Space: O(1)

---

## Q26. Custom Exception with Message — Class Predefined

**Difficulty:** HARD  
**Concept:** Custom exception constructor, `super(message)`, `getMessage()`

### PROBLEM STATEMENT
Use a predefined custom exception class `InvalidMarksException`. Its constructor must accept a message and pass it to the parent `Exception` class. Throw it with a meaningful message and print `e.getMessage()`.

### PREDEFINED CODE
```java
import java.util.*;

class InvalidMarksException extends Exception {
    InvalidMarksException(String message) {
        // Call the parent constructor
    }
}

public class Main {

    static void validateMarks(int marks)
            throws InvalidMarksException {

        // Throw exception with a suitable message
    }

    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        int marks = sc.nextInt();

        try {
            validateMarks(marks);
            System.out.println("Valid Marks");
        } catch (InvalidMarksException e) {
            System.out.println(e.getMessage());
        }
    }
}
```

### SAMPLE INPUT
```text
120
```

### SAMPLE OUTPUT
```text
Marks must be between 0 and 100
```

### EXPLANATION
This question focuses on custom exception constructors and passing a message to `Exception`.

**Expected Complexity:** Time: O(1), Space: O(1)

---

## Q27. Custom Exception + finally — Class Predefined

**Difficulty:** HARD  
**Concept:** custom exception, `try-catch-finally`

### PROBLEM STATEMENT
Create `InvalidPinException`. A PIN must contain exactly 4 digits. Throw the custom exception if it is invalid. Print `Process Completed` from `finally`.

### PREDEFINED CODE
```java
import java.util.*;

class InvalidPinException extends Exception {
    // Write constructor
}

public class Main {

    static void validatePin(String pin)
            throws InvalidPinException {

        // Validate that PIN has exactly 4 digits
    }

    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        String pin = sc.next();

        try {
            validatePin(pin);
            System.out.println("Valid PIN");
        } catch (InvalidPinException e) {
            System.out.println("Invalid PIN");
        } finally {
            System.out.println("Process Completed");
        }
    }
}
```

### SAMPLE INPUT
```text
123
```

### SAMPLE OUTPUT
```text
Invalid PIN
Process Completed
```

### EXPLANATION
This combines a custom checked exception with the complete `try-catch-finally` structure.

**Expected Complexity:** Time: O(N), Space: O(1)

---

## Q28. Multiple Custom Exceptions — Classes Predefined

**Difficulty:** HARD  
**Concept:** Multiple custom exceptions, multiple `catch`

### PROBLEM STATEMENT
A student must have marks between 0 and 100 and attendance of at least 75%. Use `InvalidMarksException` for invalid marks and `LowAttendanceException` for attendance below 75%.

### PREDEFINED CODE
```java
import java.util.*;

class InvalidMarksException extends Exception {
    // Write constructor
}

class LowAttendanceException extends Exception {
    // Write constructor
}

public class Main {

    static void checkStudent(int marks, int attendance)
            throws InvalidMarksException, LowAttendanceException {

        // Validate marks
        // Validate attendance
    }

    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        int marks = sc.nextInt();
        int attendance = sc.nextInt();

        try {
            checkStudent(marks, attendance);
            System.out.println("Student Eligible");
        } catch (InvalidMarksException e) {
            System.out.println("Invalid Marks");
        } catch (LowAttendanceException e) {
            System.out.println("Low Attendance");
        }
    }
}
```

### SAMPLE INPUT
```text
80 60
```

### SAMPLE OUTPUT
```text
Low Attendance
```

### EXPLANATION
A method can declare multiple checked exceptions. The caller can handle each type separately.

**Expected Complexity:** Time: O(1), Space: O(1)

---

## Q29. Custom Exception + Exception Propagation

**Difficulty:** HARD  
**Concept:** custom exception, `throws`, propagation

### PROBLEM STATEMENT
Create `InsufficientBalanceException` yourself. The exception should originate in `withdraw()`, propagate through `processWithdrawal()`, and finally be handled in `main()`.

### PREDEFINED CODE
```java
import java.util.*;

public class Main {

    // Create InsufficientBalanceException here

    static void withdraw(double balance, double amount)
            throws InsufficientBalanceException {

        // Throw exception when amount > balance
    }

    static void processWithdrawal(double balance, double amount)
            throws InsufficientBalanceException {

        // Call withdraw()
    }

    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        double balance = sc.nextDouble();
        double amount = sc.nextDouble();

        try {
            processWithdrawal(balance, amount);
            System.out.println("Withdrawal Successful");
        } catch (InsufficientBalanceException e) {
            System.out.println("Insufficient Balance");
        }
    }
}
```

### SAMPLE INPUT
```text
5000 8000
```

### SAMPLE OUTPUT
```text
Insufficient Balance
```

### EXPLANATION
The exception is created by the student, thrown in one method, passed through another using `throws`, and finally handled in `main()`.

**Expected Complexity:** Time: O(1), Space: O(1) excluding call stack

---

## Q30. Complete Exception Handling Challenge

**Difficulty:** HARD  
**Concept:** `try`, multiple `catch`, `finally`, `throw`, `throws`, custom exception

### PROBLEM STATEMENT
Create a small ATM validation program.

The program receives:
1. Account balance
2. Withdrawal amount
3. PIN

Create `InvalidPinException` yourself.

Rules:
- PIN must be exactly `1234`.
- Withdrawal amount must be positive.
- Withdrawal amount cannot exceed the balance.
- Invalid PIN must throw `InvalidPinException`.
- Invalid withdrawal amount must throw `IllegalArgumentException`.
- Insufficient balance must throw `ArithmeticException`.
- Handle each exception separately.
- Always print `Transaction Completed` in `finally`.
- If everything is valid, print the remaining balance.

### PREDEFINED CODE
```java
import java.util.*;

public class Main {

    // Create InvalidPinException here

    static void validatePin(int pin)
            throws InvalidPinException {

        // Throw InvalidPinException if PIN is not 1234
    }

    static double withdraw(double balance, double amount)
            throws IllegalArgumentException, ArithmeticException {

        // Throw IllegalArgumentException for amount <= 0
        // Throw ArithmeticException when amount > balance
        // Return remaining balance
        return 0;
    }

    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        double balance = sc.nextDouble();
        double amount = sc.nextDouble();
        int pin = sc.nextInt();

        try {
            // Validate PIN
            // Withdraw amount
            // Print remaining balance

        } catch (InvalidPinException e) {
            // Handle invalid PIN

        } catch (IllegalArgumentException e) {
            // Handle invalid withdrawal amount

        } catch (ArithmeticException e) {
            // Handle insufficient balance

        } finally {
            // Print "Transaction Completed"
        }
    }
}
```

### SAMPLE INPUT
```text
5000
6000
1234
```

### SAMPLE OUTPUT
```text
Insufficient Balance
Transaction Completed
```

### EXPLANATION
This is the final integrated problem. It combines:
- a custom exception created by you,
- predefined exceptions,
- `throw`,
- `throws`,
- multiple `catch` blocks,
- and `finally`.

You should be able to solve this question only after understanding the complete exception-handling flow.

**Expected Complexity:** Time: O(1), Space: O(1)

---

# CONCEPT COVERAGE CHECKLIST

| Concept | Questions |
|---|---|
| Basic `try-catch` | 1, 2, 3, 4 |
| Predefined exceptions | 1–18 |
| Multiple `catch` | 5, 7, 17, 28, 30 |
| `finally` | 6, 7, 27, 30 |
| Nested `try-catch` | 8 |
| `Exception` superclass | 9 |
| `getMessage()` | 10, 26 |
| `throw` | 11, 12, 14, 15, 17, 19–30 |
| `throws` | 13, 14, 16, 18–30 |
| Exception propagation | 18, 29 |
| Custom exception — class predefined | 19, 20, 21, 22, 26, 27, 28 |
| Custom exception — class NOT predefined | 23, 24, 25, 29, 30 |
| Custom checked exception | 19–30 |
| Custom exception constructor | 19–30 |
| Custom exception message | 26 |
| Custom + `finally` | 27, 30 |
| Multiple custom exceptions | 28 |
| Predefined + custom exceptions together | 30 |
| Complete integrated problem | 30 |

# FINAL REVISION TARGET

After completing all 30 questions, you should be comfortable with:

1. Writing `try-catch`.
2. Handling common predefined exceptions.
3. Using multiple `catch` blocks.
4. Understanding the role of `finally`.
5. Using `throw` to explicitly generate an exception.
6. Using `throws` to declare/pass an exception.
7. Understanding the difference between `throw` and `throws`.
8. Creating a custom exception using `extends Exception`.
9. Writing custom exception constructors.
10. Passing messages through `super(message)`.
11. Catching custom exceptions.
12. Propagating exceptions between methods.
13. Handling multiple checked exceptions.
14. Combining predefined and custom exceptions.
15. Solving exam-style exception-handling problems independently.
